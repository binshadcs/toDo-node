<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="main.css">
</head>
<body>
<main>
    <div>
        <h2>To Do List</h2>

    </div>
    <form action="/add" method="post">
        <input type="text" name="text" id="text">
        <input type="submit" value="add">
        <ul>
            <% if(locals.list){ %>
                <% for(value of list) {%>
                    <li>
                        <input type="checkbox" name="checked" id="checked"><del>
                        <%= value %></del>
                    </li>
                <% } %>
            <% }else{ %>
                <h4>Add To Do</h4>
            <% } %>
        </ul>
    </form>
</main>
</body>
</html>